import chai from 'chai';

const { expect } = chai;

describe('smoke test', () => {
  it('checks equality', () => {
    // eslint-disable-next-line no-unused-expressions
    expect(true).to.be.true;
  });
});
